{{ machine_name }}.settings:
  route_name: {{ machine_name }}.settings
  base_route: {{ machine_name }}.settings

{{ machine_name }}.settings_overview:
  title: Settings
  route_name: {{ machine_name }}.settings
  parent_id: {{ machine_name }}.settings
